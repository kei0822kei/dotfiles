# -*- tmux.conf -*-

TMUX_DIR=$HOME/.tmux

# change prefix
unbind C-b
set -g prefix C-o

set -g base-index 1    # start index with 1
set escape-time 0

# keybind
bind | split-window -h
bind - split-window -v
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
# bind -r C-h select-window -t :-
# bind -r C-l select-window -t :+

# synchronize
bind s setw synchronize-panes on
bind S setw synchronize-panes off

# mouse
setw -g mouse on

# colors
# set -g default-terminal "screen-256color"
set-option -g default-terminal screen-256color
set -g terminal-overrides 'xterm:colors=256'
set -g status-fg green
set -g status-bg black


# status bar
set -g status-left-length 40
set -g status-left "#[fg=green]Session: #S #[fg=yellow]#I #[fg=cyan]#P"
set -g status-right "#[fg=cyan][%Y-%m-%d(%a) %H:%M]"
set -g status-interval 60
set -g status-justify centre
setw -g monitor-activity on
set -g visual-activity on
set -g status-position top




# Vimのキーバインドでペインをリサイズする
# bind -r H resize-pane -L 5
# bind -r J resize-pane -D 5
# bind -r K resize-pane -U 5
# bind -r L resize-pane -R 5


# 256色端末を使用する

# ステータスバーの色を設定する

# ウィンドウリストの色を設定する
# setw -g window-status-fg cyan
# setw -g window-status-bg default
# setw -g window-status-attr dim
# # アクティブなウィンドウを目立たせる
# setw -g window-status-current-fg white
# setw -g window-status-current-bg red
# setw -g window-status-current-attr bright
# 
# # ペインボーダーの色を設定する
# set -g pane-border-fg green
# set -g pane-border-bg black
# # アクティブなペインを目立たせる
# set -g pane-active-border-fg white
# set -g pane-active-border-bg yellow
# 
# # コマンドラインの色を設定する
# set -g message-fg white
# set -g message-bg black
# set -g message-attr bright

# ステータスバーを設定する
## 左パネルを設定する
# コピーモードを設定する
# viのキーバインドを使用する
#setw -g mode-keys vi
# クリップボード共有を有効にする
#set-option -g default-command "reattach-to-user-namespace -l zsh"
# コピーモードの操作をvi風に設定する
#bind-key -T copy-mode-vi v send-keys -X begin-selection
#bind-key -T copy-mode-vi y send-keys copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"
#unbind -T copy-mode-vi Enter
#bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"

#bind -t vi-copyy copy-pipe "xclip -sel clip -i"


# status line
# set-option -g status on
# set-option -g pane-border-status bottom
# set-option -g pane-border-format "#[fg=black,bg=blue] #P #(tmux-pane-border '#{pane_current_path}')"
# set-option -g pane-border-fg colour023
# set-option -g pane-active-border-fg blue
# set-option -g pane-border-format "#[fg=black,bg=blue] #P #(~/.tmux/tmux-pane-border #{pane_current_path})"

# 動かない
# set-option -g status-right '#(~/.tmux/get_load_average_tmux)#(~/.tmux/get_volume_tmux)#(~/.tmux/get_battery_tmux)#(~/.tmux/get_ssid_tmux)#[fg=colour014] %H:%M | %m/%d %a#[default]'

# tmux-battery
# run-shell ~/.tmux/plugins/tmux-battery/battery.tmux
# set -g status-right '#{battery_status_bg} Batt: #{battery_icon} #{battery_percentage} #{battery_remain} | %a %h-%d %H:%M '


# set-option -g status on
# set-option -g status-interval 2
# set-option -g status-justify "centre"
# set-option -g status-left-length 60
# set-option -g status-right-length 90
# set-option -g status-left "#($TMUX_DIR/plugins/tmux-powerline/powerline.sh left)"
# set-option -g status-right "#(~/path/to/tmux-powerline/powerline.sh right)"

### plugins


# set -g @tpm_plugins '              \
#   tmux-plugins/tpm                 \
#   tmux-plugins/tmux-copycat        \
#   tmux-plugins/tmux-yank           \
#   tmux-plugins/tmux-open           \
#   tmux-plugins/tmux-resurrect      \
#   tmux-plugins/tmux-battery        \
#   tmux-plugins/tmux-pain-control   \
# '

# set -g @plugin 'tmux-plugins/tmux-battery'
# set -g @plugin 'tmux-plugins/tmux-online-status'
# set -g @plugin 'tmux-plugins/tmux-cpu'



# run-shell '~/.tmux/plugins/tpm/tpm'
